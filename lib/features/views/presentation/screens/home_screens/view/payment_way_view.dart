import 'package:flutter/material.dart';

class PayMentWayView extends StatelessWidget {
  const PayMentWayView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
         return Scaffold(
      body: Text("payment"),
    );
  }
}
